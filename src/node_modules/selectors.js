import * as R from 'ramda'

const getProductsById = (state, id) => R.prop(id, state.products)

export const getProducts = state => {
  return R.map(id => getProductsById(state, id), state.productsPage.ids)
}